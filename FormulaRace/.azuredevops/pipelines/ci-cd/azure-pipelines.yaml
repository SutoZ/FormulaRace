trigger:
  branches:
    include:
      - main
      - develop
      - feature/*
  paths:
    exclude:
      - README.md
      - docs/*
      - '*.md'

pr:
  branches:
    include:
      - main
      - develop

variables:
  buildConfiguration: 'Release'
  dotNetVersion: '9.0.x'
  vmImageName: 'ubuntu-latest'
  projectName: 'TeamManagementService'
  solutionPath: '**/*.sln'
  unitTestProjectsPath: '**/*UnitTests/*.csproj'
  integrationTestProjectsPath: '**/*IntegrationTests/*.csproj'
  apiProjectPath: 'TeamManagementService.API/TeamManagementService.API.csproj'
  testDbPassword: 'Nagyon_Nemmindegy98*?.-'
  testConnectionString: 'Server=localhost,1433;Database=RaceDbTest;User Id=sa;Password=$(testDbPassword);TrustServerCertificate=True'
  infrastructureProjectPath: 'TeamManagementService.Infrastructure/TeamManagementService.Infrastructure.csproj'

stages:
  - stage: CI
    displayName: 'Continuous Integration'
    jobs:
      - job: BuildAndTests
        displayName: 'Build & Tests'
        pool:
          vmImage: $(vmImageName)

        services:
          sqlserver:
            image: mcr.microsoft.com/mssql/server:2022-latest
            env:
              ACCEPT_EULA: Y
              SA_PASSWORD: $(testDbPassword)
              MSSQL_PID: Developer
            ports:
              - 1433:1433
            options: >-
              --health-cmd="/opt/mssql-tools18/bin/sqlcmd -S localhost -U SA -P '$(testDbPassword)' -Q 'SELECT 1' -C || exit 1"
              --health-interval=15s
              --health-timeout=10s
              --health-retries=20
              --health-start-period=90s

        steps:
          - task: UseDotNet@2
            displayName: 'Install .NET Core SDK'
            inputs:
              packageType: 'sdk'
              version: $(dotNetVersion)
              includePreviewVersions: false

          - task: Cache@2
            displayName: 'Cache NuGet packages'
            inputs:
              key: 'nuget | "$(Agent.OS)" | **/packages.lock.json,**/nuget.config'
              restoreKeys: |
                nuget | "$(Agent.OS)"
              path: $(Pipeline.Workspace)/.nuget/packages

          - task: Cache@2
            displayName: 'Cache .NET Global Tools'
            inputs:
              key: 'dotnet-tools | "$(Agent.OS)" | $(dotNetVersion)'
              restoreKeys: |
                dotnet-tools | "$(Agent.OS)"
              path: ~/.dotnet/tools

          - task: Cache@2
            displayName: 'Cache APT packages'
            inputs:
              key: 'apt | "$(Agent.OS)" | mssql-tools18'
              restoreKeys: |
                apt | "$(Agent.OS)"
              path: /var/cache/apt/archives

          - task: DotNetCoreCLI@2
            displayName: 'Install EF Core tools'
            condition: succeeded()
            inputs:
              command: 'custom'
              custom: 'tool'
              arguments: 'update --global dotnet-ef'

          - task: DotNetCoreCLI@2
            displayName: 'Install ReportGenerator'
            inputs:
              command: 'custom'
              custom: 'tool'
              arguments: 'update --global dotnet-reportgenerator-globaltool'

          - script: |
              # Install SQL Server command line tools
              # Modern way to add Microsoft repository key
              curl -sSL https://packages.microsoft.com/keys/microsoft.asc | sudo gpg --dearmor -o /usr/share/keyrings/microsoft-prod.gpg

              # Add repository with keyring reference
              echo "deb [arch=amd64,arm64,armhf signed-by=/usr/share/keyrings/microsoft-prod.gpg] https://packages.microsoft.com/ubuntu/20.04/prod focal main" | sudo tee /etc/apt/sources.list.d/msprod.list

              # Update and install
              sudo apt-get update
              sudo ACCEPT_EULA=Y apt-get install -y mssql-tools18 unixodbc-dev

              # Add to Path
              echo 'export PATH="$PATH:/opt/mssql-tools18/bin"' >> ~/.bashrc
              export PATH="$PATH:/opt/mssql-tools18/bin"

              # Verify installation
              which sqlcmd || echo "⚠️  sqlcmd not found in PATH"
              echo "✅ SQL Server tools installation completed"
            displayName: 'Install SQL Server tools'

          - task: DotNetCoreCLI@2
            displayName: 'Restore NuGet packages'
            inputs:
              command: 'restore'
              projects: $(solutionPath)
              feedsToUse: 'select'
              verbosityRestore: 'Normal'
              restoreArguments: '--packages $(Pipeline.Workspace)/.nuget/packages'

          - task: DotNetCoreCLI@2
            displayName: 'Build solution'
            inputs:
              command: 'build'
              projects: $(solutionPath)
              arguments: '--configuration $(buildConfiguration) --no-restore'

          - task: DotNetCoreCLI@2
            displayName: 'Run unit tests'
            inputs:
              command: 'test'
              projects: $(unitTestProjectsPath)
              arguments: '--configuration $(buildConfiguration) --no-build --collect:"XPlat Code Coverage" --logger trx --verbosity normal'
              publishTestResults: true
              testRunTitle: 'Unit Tests - $(Build.BuildNumber)'
            env:
              ASPNETCORE_ENVIRONMENT: 'Test'

          - script: |
              echo "Waiting for SQL Server to be ready..."
              export PATH="$PATH:/opt/mssql-tools18/bin"

              # Wait  for SQL Server to be ready (max 4 minutes)
              connected=false
              max_attempts=80

              for i in $(seq 1 $max_attempts); do
                echo "🔍 Attempt $i/$max_attempts: Testing SQL Server connection..."
                
                if sqlcmd -S localhost,1433 -U SA -P '$(testDbPassword)' -Q "SELECT 1" -C -t 10 > /dev/null 2>&1; then
                  echo "✅ SQL Server is ready!"
                  connected=true
                  break
                fi
                
                echo "⏳ SQL Server not ready yet, waiting 3 seconds..."
                sleep 3
              done

              # Verify connection was established
              if [ "$connected" != "true" ]; then
                echo "❌ SQL Server failed to start after $((max_attempts * 3)) seconds"
                echo "🔍 Debugging information:"
                docker ps -a | grep sql || echo "No SQL containers found"
                docker logs sqlserver 2>/dev/null | tail -30 || echo "Cannot get container logs"
                exit 1
              fi

              echo "📋 Creating test database..."
              sqlcmd -S localhost,1433 -U SA -P '$(testDbPassword)' -C -Q "
              IF NOT EXISTS (SELECT name FROM sys.databases WHERE name = 'RaceDbTest')
              BEGIN
                  CREATE DATABASE RaceDbTest;
                  PRINT 'RaceDbTest database created successfully ✅';
              END
              ELSE
              BEGIN
                  PRINT 'RaceDbTest database already exists ℹ️';
              END
              "

              # Verify database creation
              if [ $? -eq 0 ]; then
                echo "🎯 Database setup completed successfully!"
              else
                echo "❌ Database creation failed"
                exit 1
              fi
            displayName: 'Setup test database'
            condition: succeeded()

          - task: DotNetCoreCLI@2
            displayName: 'Apply database migrations'
            inputs:
              command: 'custom'
              custom: 'ef'
              arguments: 'database update --project $(infrastructureProjectPath) --startup-project $(apiProjectPath) --configuration $(buildConfiguration) --verbosity normal'
            env:
              ConnectionStrings__RaceConnection: $(testConnectionString)
              ASPNETCORE_ENVIRONMENT: 'Test'
            condition: succeeded()

          - task: DotNetCoreCLI@2
            displayName: 'Run integration tests'
            inputs:
              command: 'test'
              projects: $(integrationTestProjectsPath)
              arguments: '--configuration $(buildConfiguration) --no-build --collect:"XPlat Code Coverage" --logger trx --verbosity normal'
              publishTestResults: true
              testRunTitle: 'Integration Tests - $(Build.BuildNumber)'
            env:
              ConnectionStrings__RaceConnection: $(testConnectionString)
              ASPNETCORE_ENVIRONMENT: 'Test'
            condition: succeeded()

          - task: DotNetCoreCLI@2
            displayName: 'Generate coverage report'
            inputs:
              command: 'custom'
              custom: 'reportgenerator'
              arguments: '-reports:$(Agent.TempDirectory)/TestResults/**/coverage.cobertura.xml -targetdir:$(Agent.TempDirectory)/TestResults/CoverageReport -reporttypes:"HtmlInline_AzurePipelines;Cobertura;Badges"'
            condition: succeededOrFailed()

          - task: PublishCodeCoverageResults@2
            displayName: 'Publish enhanced code coverage'
            inputs:
              summaryFileLocation: '$(Agent.TempDirectory)/TestResults/CoverageReport/Cobertura.xml'
              reportDirectory: '$(Agent.TempDirectory)/TestResults/CoverageReport'
              pathToSources: '$(Build.SourcesDirectory)'
              failIfCoverageEmpty: false
            condition: succeededOrFailed()

          - task: DotNetCoreCLI@2
            displayName: 'Publish API application'
            inputs:
              command: 'publish'
              publishWebProjects: false
              projects: $(apiProjectPath)
              arguments: '--configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)/api --no-restore --no-build'

          - task: CopyFiles@2
            displayName: 'Copy deployment files'
            inputs:
              SourceFolder: '$(Build.SourcesDirectory)'
              Contents: |
                Dockerfile
                docker-compose.yml
                appsettings*.json
                **/*.dacpac
              TargetFolder: '$(Build.ArtifactStagingDirectory)/deploy'
            condition: succeededOrFailed()

          - task: PublishBuildArtifacts@1
            displayName: 'Publish build artifacts'
            inputs:
              PathtoPublish: '$(Build.ArtifactStagingDirectory)'
              ArtifactName: '$(projectName)-$(Build.BuildNumber)'
              publishLocation: 'Container'
            condition: succeededOrFailed()

      - job: CodeQuality
        displayName: 'Code Quality Analysis'
        pool:
          vmImage: $(vmImageName)
        dependsOn: BuildAndUnitTest # ← Fixed dependency name
        condition: succeeded()

        steps:
          - task: UseDotNet@2
            displayName: 'Install .NET Core SDK'
            inputs:
              packageType: 'sdk'
              version: $(dotNetVersion)

          # ✅ Reuse NuGet cache
          - task: Cache@2
            displayName: 'Cache NuGet packages'
            inputs:
              key: 'nuget | "$(Agent.OS)" | **/packages.lock.json,**/nuget.config'
              restoreKeys: |
                nuget | "$(Agent.OS)"
              path: $(Pipeline.Workspace)/.nuget/packages

          - task: DotNetCoreCLI@2
            displayName: 'Restore packages for analysis'
            inputs:
              command: 'restore'
              projects: $(solutionPath)
              restoreArguments: '--packages $(Pipeline.Workspace)/.nuget/packages'

          # ✅ Add missing build step
          - task: DotNetCoreCLI@2
            displayName: 'Build solution for analysis'
            inputs:
              command: 'build'
              projects: $(solutionPath)
              arguments: '--configuration $(buildConfiguration) --no-restore'

          - task: DotNetCoreCLI@2
            displayName: 'Run security vulnerability scan'
            inputs:
              command: 'custom'
              custom: 'list'
              arguments: 'package --vulnerable --include-transitive'
              projects: $(solutionPath)
            continueOnError: true
# Placeholder for future CD stages - uncomment and configure when needed
# - stage: Deploy_Dev
#   displayName: 'Deploy to Development'
#   dependsOn: CI
#   condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/develop'))
#   jobs:
#   - deployment: DeployDev
#     displayName: 'Deploy to Dev Environment'
#     pool:
#       vmImage: $(vmImageName)
#     environment: 'Development'
#     strategy:
#       runOnce:
#         deploy:
#           steps:
#           - template: deploy-template.yml
#             parameters:
#               environment: 'dev'
#               azureSubscription: 'Azure-ServiceConnection-Dev'
#               appServiceName: 'teammanagement-dev'

# - stage: Deploy_Prod
#   displayName: 'Deploy to Production'
#   dependsOn: CI
#   condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
#   jobs:
#   - deployment: DeployProd
#     displayName: 'Deploy to Production Environment'
#     pool:
#       vmImage: $(vmImageName)
#     environment: 'Production'
#     strategy:
#       runOnce:
#         deploy:
#           steps:
#           - template: deploy-template.yml
#             parameters:
#               environment: 'prod'
#               azureSubscription: 'Azure-ServiceConnection-Prod'
#               appServiceName: 'teammanagement-prod'
